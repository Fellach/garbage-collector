{
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch",
    "build": "rm -f *.zip && zip -qr zg6.zip . && aws lambda update-function-code --function-name zg6 --zip-file fileb://zg6.zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsdom": "^16.4.0"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
